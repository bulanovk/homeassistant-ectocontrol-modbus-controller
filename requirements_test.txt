# Test dependencies for Ectocontrol Modbus integration
# Install with: pip install -r requirements_test.txt
-r requirements.txt

# Testing framework
pytest>=9.0.2
pytest-asyncio>=1.3.0
pytest-cov>=6.0
